# image=ubuntu-gnome-minimal.box
# ./ln.sh
# vagrant up
# vagrant halt
# vagrant package --output $image
# vagrant box add Ubuntu23-Gnome-minimal $image
# vagrant box list

# vagrant destroy -f

echo `dirname`